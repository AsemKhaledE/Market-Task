@model List<Market.ViewModel.UserViewModel>
@{
    var userModel = new Market.ViewModel.UserViewModel();
}

<div class="container-fluid">
    <div class="row">
        <div>
            <partial name="AddUserOfList" model='@userModel' />
        </div>
    </div>
</div>

<script src="~/lib/jquery/dist/jquery.js"></script>
<table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>FullName</th>
            <th> Email</th>
            <th>Phone</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Id)</td>
                <td>@Html.DisplayFor(modelItem => item.FullName)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.Phone)</td>
                <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a></td>
                <td><a class="btn btn-primary" asp-action="EditUser" asp-route-id="@item.Id">Update</a></td>
            </tr>
        }
    </tbody>
</table>